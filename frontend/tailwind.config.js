// Tailwind CSS v4 detects config from CSS files, this file is mostly not needed but kept for compatibility or specific v3-style plugins if any.
// The theme configuration has been moved to src/index.css
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}
